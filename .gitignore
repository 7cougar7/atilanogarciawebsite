.idea/
.env
*__pycache__/
desktop.ini
db.sqlite3
media/
venv/
.gitignore
staticfiles/db.sqlite3
.vscode/*
staticfiles/*
phone-translator-*.json
fontawesome-free-6.7.2-web/
mainwebsite/static/fontawesome/*
# FontAwesome extracted files (keep only the zip)
mainwebsite/static/fontawesome/
!mainwebsite/static/fontawesome-free-6.7.2.zip

# Node / Vite. node_modules is rebuilt via `npm install`. The built bundle in
# mainwebsite/static/dist/ (including .vite/manifest.json) IS committed, because
# production has no Node and relies on collectstatic picking up the prebuilt assets.
node_modules/
npm-debug.log*
